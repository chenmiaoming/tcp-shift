diff --git a/src/core/tcp.c b/src/core/tcp.c
--- a/src/core/tcp.c
+++ b/src/core/tcp.c
@@ -1294,13 +1294,16 @@ tcp_slowtmr(void)
             /* Reset the retransmission timer. */
             pcb->rtime = 0;
 
-            /* Reduce congestion window and ssthresh. */
-            eff_wnd = LWIP_MIN(pcb->cwnd, pcb->snd_wnd);
-            pcb->ssthresh = eff_wnd >> 1;
-            if (pcb->ssthresh < (tcpwnd_size_t)(pcb->mss << 1)) {
-              pcb->ssthresh = (tcpwnd_size_t)(pcb->mss << 1);
+            /* Delegate base congestion policy when a project hook is bound.
+             * Retransmission preparation/commit remains native lwIP. */
+            if (!tcp_shift_lwip_cc_hook_timeout(pcb)) {
+              eff_wnd = LWIP_MIN(pcb->cwnd, pcb->snd_wnd);
+              pcb->ssthresh = eff_wnd >> 1;
+              if (pcb->ssthresh < (tcpwnd_size_t)(pcb->mss << 1)) {
+                pcb->ssthresh = (tcpwnd_size_t)(pcb->mss << 1);
+              }
+              pcb->cwnd = pcb->mss;
             }
-            pcb->cwnd = pcb->mss;
             LWIP_DEBUGF(TCP_CWND_DEBUG, ("tcp_slowtmr: cwnd %"TCPWNDSIZE_F
                                          " ssthresh %"TCPWNDSIZE_F"\n",
                                          pcb->cwnd, pcb->ssthresh));
diff --git a/src/core/tcp_in.c b/src/core/tcp_in.c
--- a/src/core/tcp_in.c
+++ b/src/core/tcp_in.c
@@ -1121,4 +1121,5 @@ tcp_free_acked_segments(struct tcp_pcb *pcb, struct tcp_seg *seg_list, const cha
     pcb->snd_queuelen = (u16_t)(pcb->snd_queuelen - clen);
     recv_acked = (tcpwnd_size_t)(recv_acked + next->len);
+    tcp_shift_lwip_cc_hook_segment_acked(pcb, next, next->len);
     tcp_seg_free(next);
 
@@ -1222,4 +1223,6 @@ tcp_receive(struct tcp_pcb *pcb)
               if (pcb->dupacks > 3) {
                 /* Inflate the congestion window */
-                TCP_WND_INC(pcb->cwnd, pcb->mss);
+                if (!tcp_shift_lwip_cc_hook_recovery_controller_owned(pcb)) {
+                  TCP_WND_INC(pcb->cwnd, pcb->mss);
+                }
               }
@@ -1238,7 +1241,9 @@ tcp_receive(struct tcp_pcb *pcb)
       /* Reset the "IN Fast Retransmit" flag, since we are no longer
          in fast retransmit. Also reset the congestion window to the
          slow start threshold. */
       if (pcb->flags & TF_INFR) {
+        if (!tcp_shift_lwip_cc_hook_recovery_exit(pcb)) {
+          pcb->cwnd = pcb->ssthresh;
+        }
         tcp_clear_flags(pcb, TF_INFR);
-        pcb->cwnd = pcb->ssthresh;
         pcb->bytes_acked = 0;
@@ -1249,22 +1254,24 @@ tcp_receive(struct tcp_pcb *pcb)
       /* Update the congestion control variables (cwnd and
          ssthresh). */
       if (pcb->state >= ESTABLISHED) {
-        if (pcb->cwnd < pcb->ssthresh) {
-          tcpwnd_size_t increase;
-          /* limit to 1 SMSS segment during period following RTO */
-          u8_t num_seg = (pcb->flags & TF_RTO) ? 1 : 2;
-          /* RFC 3465, section 2.2 Slow Start */
-          increase = LWIP_MIN(acked, (tcpwnd_size_t)(num_seg * pcb->mss));
-          TCP_WND_INC(pcb->cwnd, increase);
-          LWIP_DEBUGF(TCP_CWND_DEBUG, ("tcp_receive: slow start cwnd %"TCPWNDSIZE_F"\n", pcb->cwnd));
-        } else {
-          /* RFC 3465, section 2.1 Congestion Avoidance */
-          TCP_WND_INC(pcb->bytes_acked, acked);
-          if (pcb->bytes_acked >= pcb->cwnd) {
-            pcb->bytes_acked = (tcpwnd_size_t)(pcb->bytes_acked - pcb->cwnd);
-            TCP_WND_INC(pcb->cwnd, pcb->mss);
+        if (!tcp_shift_lwip_cc_hook_ack(pcb, acked)) {
+          if (pcb->cwnd < pcb->ssthresh) {
+            tcpwnd_size_t increase;
+            /* limit to 1 SMSS segment during period following RTO */
+            u8_t num_seg = (pcb->flags & TF_RTO) ? 1 : 2;
+            /* RFC 3465, section 2.2 Slow Start */
+            increase = LWIP_MIN(acked, (tcpwnd_size_t)(num_seg * pcb->mss));
+            TCP_WND_INC(pcb->cwnd, increase);
+            LWIP_DEBUGF(TCP_CWND_DEBUG, ("tcp_receive: slow start cwnd %"TCPWNDSIZE_F"\n", pcb->cwnd));
+          } else {
+            /* RFC 3465, section 2.1 Congestion Avoidance */
+            TCP_WND_INC(pcb->bytes_acked, acked);
+            if (pcb->bytes_acked >= pcb->cwnd) {
+              pcb->bytes_acked = (tcpwnd_size_t)(pcb->bytes_acked - pcb->cwnd);
+              TCP_WND_INC(pcb->cwnd, pcb->mss);
+            }
+            LWIP_DEBUGF(TCP_CWND_DEBUG, ("tcp_receive: congestion avoidance cwnd %"TCPWNDSIZE_F"\n", pcb->cwnd));
           }
-          LWIP_DEBUGF(TCP_CWND_DEBUG, ("tcp_receive: congestion avoidance cwnd %"TCPWNDSIZE_F"\n", pcb->cwnd));
         }
       }
       LWIP_DEBUGF(TCP_INPUT_DEBUG, ("tcp_receive: ACK for %"U32_F", unacked->seqno %"U32_F":%"U32_F"\n",
diff --git a/src/core/tcp_out.c b/src/core/tcp_out.c
--- a/src/core/tcp_out.c
+++ b/src/core/tcp_out.c
@@ -1341,6 +1341,11 @@ tcp_output(struct tcp_pcb *pcb)
         ((pcb->flags & (TF_NAGLEMEMERR | TF_FIN)) == 0)) {
       break;
     }
+    if ((seg->len != 0U) &&
+        !tcp_shift_lwip_cc_hook_segment_send_eligible(pcb, seg->len)) {
+      break;
+    }
+
 #if TCP_CWND_DEBUG
     LWIP_DEBUGF(TCP_CWND_DEBUG, ("tcp_output: snd_wnd %"TCPWNDSIZE_F", cwnd %"TCPWNDSIZE_F", wnd %"U32_F", effwnd %"U32_F", seq %"U32_F", ack %"U32_F", i %"S16_F"\n",
                                  pcb->snd_wnd, pcb->cwnd, wnd,
@@ -1618,5 +1623,10 @@ tcp_output_segment(struct tcp_seg *seg, struct tcp_pcb *pcb, struct netif *netif
   err = ip_output_if(seg->p, &pcb->local_ip, &pcb->remote_ip, pcb->ttl,
                      pcb->tos, IP_PROTO_TCP, netif);
   NETIF_RESET_HINTS(netif);
+
+  if (err == ERR_OK) {
+    tcp_shift_lwip_cc_hook_segment_tx(pcb, seg,
+                                      lwip_ntohl(seg->tcphdr->seqno), seg->len);
+  }
 
 #if TCP_CHECKSUM_ON_COPY
@@ -1796,18 +1806,26 @@ tcp_rexmit_fast(struct tcp_pcb *pcb)
                  (u16_t)pcb->dupacks, pcb->lastack,
                  lwip_ntohl(pcb->unacked->tcphdr->seqno)));
     if (tcp_rexmit(pcb) == ERR_OK) {
-      /* Set ssthresh to half of the minimum of the current
-       * cwnd and the advertised window */
-      pcb->ssthresh = LWIP_MIN(pcb->cwnd, pcb->snd_wnd) / 2;
-
-      /* The minimum value for ssthresh should be 2 MSS */
-      if (pcb->ssthresh < (2U * pcb->mss)) {
-        LWIP_DEBUGF(TCP_FR_DEBUG,
-                    ("tcp_receive: The minimum value for ssthresh %"TCPWNDSIZE_F
-                     " should be min 2 mss %"U16_F"...\n",
-                     pcb->ssthresh, (u16_t)(2 * pcb->mss)));
-        pcb->ssthresh = 2 * pcb->mss;
+      if (!tcp_shift_lwip_cc_hook_loss(pcb, pcb->mss)) {
+        /* Set ssthresh to half of the minimum of the current
+         * cwnd and the advertised window */
+        pcb->ssthresh = LWIP_MIN(pcb->cwnd, pcb->snd_wnd) / 2;
+
+        /* The minimum value for ssthresh should be 2 MSS */
+        if (pcb->ssthresh < (2U * pcb->mss)) {
+          LWIP_DEBUGF(TCP_FR_DEBUG,
+                      ("tcp_receive: The minimum value for ssthresh %"TCPWNDSIZE_F
+                       " should be min 2 mss %"U16_F"...\n",
+                       pcb->ssthresh, (u16_t)(2 * pcb->mss)));
+          pcb->ssthresh = 2 * pcb->mss;
+        }
       }
 
+      /* Reno/CUBIC retain native fast-recovery inflation. A controller that
+       * explicitly owns recovery cwnd (internal BBR) keeps its published
+       * packet-conservation window instead. */
+      if (!tcp_shift_lwip_cc_hook_recovery_controller_owned(pcb)) {
+        pcb->cwnd = pcb->ssthresh + 3 * pcb->mss;
+      }
       tcp_set_flags(pcb, TF_INFR);
 
